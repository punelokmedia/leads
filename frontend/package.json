{
  "name": "leads-sell-frontend",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:admin": "npm run dev -w admin-web",
    "dev:user": "npm run dev -w user-web",
    "build:admin": "npm run build -w admin-web",
    "build:user": "npm run build -w user-web",
    "lint:admin": "npm run lint -w admin-web",
    "lint:user": "npm run lint -w user-web"
  }
}
